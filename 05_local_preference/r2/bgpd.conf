! bgpd configuration file for router r2
! This file is automatically generated

hostname r2
password zpassword
enable password epassword

log file /home/mininet/git_workspace/bgp_cases/05_local_preference/r2/bgpd.log

router bgp 300
bgp router-id 170.10.0.2

! These are the networks we announce; configured here to be ALL directly connected networks
network 170.10.0.0/24
network 3.3.3.0/24

!These are the neighbors with which we estabish BGP sessions:
neighbor 170.10.0.1 remote-as 100
neighbor 3.3.3.3 remote-as 256
